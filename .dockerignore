.git
.github
.venv
venv
__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.ruff_cache
.mypy_cache
.coverage
htmlcov
# tests and deploy/k8s are deliberately NOT excluded: the `test` stage in
# deploy/Dockerfile copies both, because the manifests are checked for internal
# consistency by the suite. Only that stage copies them, so the runtime image still
# ships without either.
docs
data
.env
.DS_Store
.idea
.vscode
